#include "course.h"

/* Init universityPtr to NULL */
University* University::universityPtr = NULL;

int main()
{
    /*    
    University * uniPtr = University::get_university_ptr();
    Course math("Math", "M154F", 2);
    
    Professor ahmed;
    ahmed.name = "Ahmed Mohsen";

    uniPtr->add_course("Math", "M154F", 4, ahmed);
    cout<<(uniPtr->get_courses())[0].get_name();
    */

/*
    cout<<"Course : "<<math.get_name()<<endl;
    cout<<"Code   : "<<math.get_code()<<endl;
    cout<<"Hours  : "<<math.get_hours()<<endl;

    
    math.set_professor(ahmed);
    cout<<"Prof N.: "<<math.get_professor()->name<<endl;

    Student tarek;
    tarek.name = "Tarook";
    math.set_student(tarek);

    Student shenawy;
    shenawy.name = "Shenawy";
    math.set_student(shenawy);

    math.set_student(tarek);

    cout<<"Student[0]: "<<math.get_student(0)->name<<endl;
    cout<<"Student[1]: "<<math.get_student(1)->name<<endl;
    cout<<"Student[2]: "<<math.get_student(2)->name<<endl;

    cout<<"***************************************"<<endl;
    
    math.remove_student();

    int i = 0;
    while(math.get_students()[i] != NULL)
    {
        cout<<"Student["<<i<<"]: "<<math.get_students()[i]->name<<endl;
        i++;
    }


    if(math.get_student(3) == NULL)
        cout<<"NULL";
    else
        cout<<"NOT NULL";

*/
    return 0;
}